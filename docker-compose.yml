version: "3.9"

services:

  mongodb:
    image: mongo:8

    container_name: nexus-mongodb

    restart: always

    ports:
      - "27017:27017"

    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data: